sudo: required
services:
    - docker
before_install:
    - docker build -t garyclarke/dockerreact -f Dockerfile.dev .
script:
    - docker run -e CI=true garyclarke/dockerreact npm run test -- --coverage
language: generic
